{
  "name": "ai-runtime",
  "version": "1.0.0",
  "description": "AI runtime on the client",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --recursive dev",
    "build": "pnpm --recursive build",
    "build:core": "pnpm --filter @ai-runtime/core build",
    "build:react": "pnpm --filter @ai-runtime/react build",
    "test": "pnpm --recursive test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
